# Copia este archivo como ".env" (sin .example) y rellena tus valores.
# Los encuentras en Supabase -> Settings -> API

# Project URL
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co

# Project API keys -> anon public  (la PÚBLICA, no la service_role)
VITE_SUPABASE_ANON_KEY=tu-anon-key-aqui

# Número de WhatsApp para pedidos (formato internacional sin +, ej: 51938417243)
VITE_WHATSAPP_NUMERO=tu_numero_aqui

# Izipay se habilita en el frontend con un booleano. Todas las credenciales,
# incluida la public key de Krypton, las entrega la Edge Function server-side.
VITE_IZIPAY_ENABLED=false

# Login con Google (OAuth). Pon 'true' para mostrar el botón "Continuar con
# Google". Requiere tener el proveedor Google habilitado en Supabase.
VITE_GOOGLE_OAUTH_ENABLED=false
